# Workspace Rules

- Whenever you create or update a DSA problem markdown file under `src/content/`, always make sure to include a YouTube search link query for that problem in the `youtube:` field of the frontmatter metadata (e.g., `youtube: "https://www.youtube.com/results?search_query=<problem_name>"`).
